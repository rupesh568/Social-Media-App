const ErrorMessage = ({handleronClick}) => {
    return (
        <center className="heading">
            <h1>
                There are no posts
            </h1>
            {/* <button type="button" class="btn btn-primary"
            onClick={handleronClick}
            >Get Post From Server</button> */}
        </center>
    );
};
export default ErrorMessage;
